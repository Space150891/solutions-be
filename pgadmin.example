# Postgres settings + used in docker-compose.yml
POSTGRES_HOST="localhost"
POSTGRES_PORT="5433"
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# pgAdmin + used in docker-compose.yml
PGADMIN_DEFAULT_EMAIL=
PGADMIN_DEFAULT_PASSWORD=